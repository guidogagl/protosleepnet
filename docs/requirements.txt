# Documentation build dependencies — import-free.
# sphinx-autoapi parses src/protosleepnet statically, so the docs build never
# imports the package and pulls NO physioex / torch. Mirrors the pyproject
# "[docs]" extra, but installed WITHOUT "-e ." so the heavy core deps stay out.
sphinx==8.1.3
pydata-sphinx-theme==0.16.1
myst-parser>=4,<5
sphinx-autoapi>=3.3,<4
sphinx-design==0.6.1
sphinx-copybutton==0.5.2
sphinxcontrib-mermaid==1.0.0
