name = "protosleepnet-demo"
compatibility_date = "2024-11-01"
pages_build_output_dir = "dist"

# DATASET_REPO is a plain variable; HF_TOKEN is set as a secret via
#   npx wrangler pages secret put HF_TOKEN --project-name protosleepnet-demo
[vars]
DATASET_REPO = "4rooms/protosleepnet-demo-data"
